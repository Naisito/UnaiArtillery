# Binarios locales de validación C++
build/

# Node / web (web/.gitignore cubre lo suyo, esto es cinturón y tirantes)
node_modules/
dist/
.env

# Unreal (capa legado)
Binaries/
DerivedDataCache/
Intermediate/
Saved/
*.sln

# Fotos y videos exportados del banco de pruebas (tools/capture.mjs)
capturas/

# SO
Thumbs.db
.DS_Store
